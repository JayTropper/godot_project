extends Node

@export var mouse_sensitivity = 0.3  # Default sensitivity
@export var volume_level = -10.0  # Default volume level (dB)
var resolution_index = 2  # Default to 1920x1080
var windowed_mode = false

func _ready():
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
